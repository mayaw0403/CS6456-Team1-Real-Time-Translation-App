// Firebase API (Copy from firebase)
const firebaseConfig = {
  apiKey: YOUR_API_KEY,
  authDomain: YOUR_AUTH_DOMAIN,
  projectId: YOUR_PROJECT_ID,
  storageBucket: YOUR_STORAGE_BUKKET,
  messagingSenderId: YOUR_SENDER_ID,
  appId: YOUR_APP_ID
};

// ChatEngine API
const projectID = YOUR_CHAT_ENGINE_API_KEY;

export { firebaseConfig, projectID };
